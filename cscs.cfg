function    = функция
include     = включить
if          = если
else        = иначе
elif        = иначе_если
return      = вернуться
print       = печать
size        = размер
while       = пока

function    = función
include     = incluir
if          = si
else        = sino
elif        = sino_si
return      = regresar
print       = imprimir
size        = tamaño
while       = mientras

ls          = dir
cp          = copy
mv          = move
rm          = rmdir
grep        = findstr
